version: '3.8'

services:
  alpha-stack-pro:
    build: .
    container_name: alpha-stack-pro
    volumes:
      - ./data:/app/data
    environment:
      - TZ=UTC
      - BINANCE_API_KEY=${BINANCE_API_KEY}
      - BINANCE_API_SECRET=${BINANCE_API_SECRET}
    restart: unless-stopped
